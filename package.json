{
  "name": "meepiap-web",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler d1 migrations apply koommai-db --remote && wrangler deploy",
    "db:local": "wrangler d1 migrations apply koommai-db --local",
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check.mjs",
    "admin:password-hash": "node scripts/admin-password-hash.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=24"
  }
}
